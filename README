This is my .vim directory.

        $ cd
        $ git clone git://github.com/pbevin/vim.git .vim
        $ ln -s .vim/vimrc .vimrc
        $ ln -s .vim/gvimrc .gvimrc

I use Macvim mostly to edit Rails.
